#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T;
    long long n;
    int fishyard;
    int N;
    scanf("%d",&T);
    while(T--)
    {
        scanf("%lld",&n);
        while(n--)
        {
            scanf("%d",&fishyard);
            scanf("%d",&N);
            int i;
            for(i=n;i>=0;i--)
            {

            }

        }


    }
    return 0;
}
