#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T;
    scanf("%d",&T);
    while(T--)
    {
        int N,M;
        scanf("%d%d",&N,&M);
        int t[111111];
        for(i=0;i<N;i++)
        {
            scanf("%d",t[i]);
        }


    }
    return 0;
}
