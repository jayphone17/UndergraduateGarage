#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <algorithm>
using namespace std;
int a[10005];

int main()
{
    int n;
    while(~scanf("%d",&n))
    {
        if(n==0)
            printf("0\n");
        else
        {
            int i;
            for(i=0; i<n; i++)
            {
                scanf("%d",a[i]);
            }
            sort (a,a+n);
            if()
            {

            }
            else if ()
            {

            }
            else if()
            {

            }
            printf();
        }
    }
    return 0;
}
