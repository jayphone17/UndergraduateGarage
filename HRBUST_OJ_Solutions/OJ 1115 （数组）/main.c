#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int array[5000];
    a[1]=0;
    a[2]=1;
    for(int i=3;i<5000;i++)
    {
        a[i]=a[i-1]+a[i-2];
    }
    int m,n;

    return 0;
}
